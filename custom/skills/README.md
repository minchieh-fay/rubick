# Custom Skills

This directory is for user-defined skills.

Create a new directory under `custom/skills/` with:
- `SKILL.md` - Skill description and instructions
- Supporting files as needed

## Structure

```
custom/skills/my-skill/
  SKILL.md       # Required: Skill description
  help.ts        # Optional: Helper functions
  ...            # Other supporting files
```
