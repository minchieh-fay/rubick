# Agent SDK project guidance

This project uses OpenAI Agents SDK with TypeScript.

For any task involving `@openai/agents`, use the local Codex skill at:

`.codex/skills/openai-agents-sdk/SKILL.md`

The skill points to the pinned, cleaned official documentation and TypeScript examples. Read only the relevant references. Check the target project's installed SDK version before copying an example. When the snapshot is older or the API is uncertain, verify against the official OpenAI documentation or upstream GitHub repository.
