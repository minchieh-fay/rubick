{
  "name": "rubick",
  "version": "0.1.0",
  "private": true,
  "description": "Personal AI assistant workspace - plugin-based tool/skill/agent ecosystem",
  "type": "module",
  "scripts": {
    "dev": "bun run server/index.ts",
    "build": "bun build server/index.ts --outfile dist/server.js --target node",
    "electron": "electron electron/main.js",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "electron": "^28.0.0",
    "typescript": "^5.3.0"
  }
}
