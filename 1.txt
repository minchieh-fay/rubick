curl http://localhost:11434/api/chat -d '{
  "model": "gemma4:e4b",
  "messages": [
    { "role": "system", "content": "直接回答问题，不要进行思考过程或输出 <thought> 标签。" },
    { "role": "user", "content": "你好，简单介绍下你自己。不超过20个字"}
  ],
  "stream": false
}'

上面的curl是可以执行的

1. 将我们ollama的默认模型改成gemma4:e4b

2. providers: 我们可以单独测试, bun运行他后
我最终要的效果:
当我运行
curl http://localhost:3000/llm/chat -d '{
  "messages": [
    { "role": "system", "content": "直接回答问题，不要进行思考过程或输出 <thought> 标签。" },
    { "role": "user", "content": "你好，简单介绍下你自己。不超过20个字"}
  ],
  "stream": false
}'
能正常返回结果, 模型应该在ollama模块自动加入