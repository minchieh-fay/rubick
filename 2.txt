我们要设计一个机制, 这个机制:

server的index.ts在监听完毕后

他要加载 加载 core和modules下的模块
而这些模块 每个模块都会有2个函数:
joinMCP
joinHTTP

server加载调用每个模块后, 他们会将自己内部的函数(http或者mcp)自动注册到server的3000端口中

mcp我不熟悉

http的话 server会创建一个/api的主地址
他的子uri由模块的joinHTTP决定, 例如user模块, 他内部自己决定是: /api/user 还是 /api/userx
包括他内部可以自己再定子uri

这个规则 写入到我们的docs下